namespace IC2.Cli;

/// <summary>
/// Placeholder entry point so IC2.Cli is a compilable, empty console app. T01 (build scaffolding
/// and CI) only pre-declares this project in IC2.sln; T23 (command layer + CLI) owns its real
/// contents. See docs/build-orchestration-plan.md.
/// </summary>
internal static class Program
{
    private static void Main()
    {
    }
}
