<Project Sdk="Microsoft.NET.Sdk">

  <!--
    T01 pre-declares this project (docs/build-orchestration-plan.md "T01 Build scaffolding and CI")
    so no later task has to edit IC2.sln. TargetFramework/Nullable/ImplicitUsings/
    TreatWarningsAsErrors come from the repo-root Directory.Build.props (conditioned on this
    project's name). Real contents belong to T23 (command layer + CLI) - this project
    intentionally contains only a placeholder entry point so `dotnet build IC2.sln` succeeds.
  -->

  <PropertyGroup>
    <OutputType>Exe</OutputType>
  </PropertyGroup>

</Project>
