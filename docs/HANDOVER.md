# Project handover — 14 September 2026 (mid-build, session restarting as Opus)

This is the starting point for the next Imperial Conquest 2 session. The goal is a **modern, playable reimplementation** that uses data from a user's original installation without checking original game files into this repository. The build is genuinely underway now — 7 of 31 tasks merged, one more written and dispatchable, one suspended pending a correction — but there is still **nothing playable**: no CLI, no Godot gameplay screens, just the engine's foundation and the first layer of rules on top of it.

> **This repository split in two.** All reverse-engineering research — `docs/reports/`, `docs/roadmap.md`, `docs/decompilation-plan.md`, `docs/research.md` — lives at **[diegoami/imperial-conquest-2-research](https://github.com/diegoami/imperial-conquest-2-research)**. *This* repository (`imperial_conquest_2`) keeps the game design, the build/release plans, and all code — it's the development repo. Links to reports/roadmap/decompilation-plan/research point at the research repo; links to `game-design.md`, `design-audit.md`, `build-orchestration-plan.md`, `release-plan.md` stay relative.

> **Where the build actually is, right now.** Merged: T01, T05 (scaffolding, CI, hygiene), T02 (domain model), T04 (fixtures corpus, now 47 reports' worth), T03 (engine seams), T30 (fixed two real `IC2.Data` parser defects found by investigation work), T06 (calendar/turn sequencing, including a corrected turn-phase order inherited from T03). **T07 (strength functions) is written and reviewed but suspended, `status:blocked`** — its review found T02's shipped `Ruleset.Siege` has three defender-strength field weights swapped, and per this project's standing process (see `build-orchestration-plan.md` §6.7, "the bug list") that gets its own correction task rather than an inline patch. **T31 fixes it, is `status:ready`, and is the single thing blocking T07** — dispatch it next, alone, before anything else in the current wave. Two smaller, non-blocking defects the same investigation found are filed as bugs, [#46](https://github.com/diegoami/imperial_conquest_2/issues/46) and [#47](https://github.com/diegoami/imperial_conquest_2/issues/47) — not yet triaged into the plan by a planner pass. Also merged since Phase 0: two evidence investigations (`docs/investigations/thracia-supply-morale.md`, confirming supply drives army morale every turn; `docs/investigations/dat-file-layout.md`, decompiling the DAT's own layout; `docs/investigations/siege-defender-strength.md`, the T31 evidence) and a rout-mechanic/battle-freeze pass that **fully closed the long-standing battle-freeze investigation** (it was a settable per-nation delay preference all along, not a bug). `dotnet build IC2.sln` / `dotnet test IC2.sln` are green on `main`.

> **Two standing mechanisms exist now, both installed as real Claude Code skills** (`.claude/skills/`, local, git-ignored — reinstall verbatim from their fenced blocks in the plan doc if ever missing): **`/build-tick`** (`build-orchestration-plan.md` Appendix C) runs one orchestration tick — reconcile, drain, dispatch one task, report; **`/process-evidence [path]`** (`docs/evidence-pipeline.md`) turns new play evidence (saves/recordings/notes) into research-repo findings, then evaluates game-design implications, as two sequential Opus dispatches. Neither existed as an actual installed skill before this session — both were invoked as `/build-tick`/`/process-evidence` and errored as unknown, so every tick this session ran by following the plan text by hand. They should work as real slash-invoked skills from here.

> **Process rules that now stand, learned the hard way this session** — all in `build-orchestration-plan.md`: **label-based review** (`gh pr review` can't work when every agent shares one GitHub account — a `status:approved`/`status:rework` label is the signal); **no worktree isolation for pipeline agents** (exactly one code-modifying agent at a time in the shared checkout); **the orchestrator/planner session itself must isolate its *own* writes** — check `ListAgents` before touching the shared checkout for anything, tracked or not, and do the work in a separate `git worktree` (on its own new branch by default, merged to `main` only for routine same-tick doc-sync or on explicit instruction) if any pipeline agent is active; and **§6.7, the bug list** — a defect found in already-merged code is never patched from inside another task's Owns list, even narrowly; it's suspended, filed as a `bug`-labelled issue, and planned into the catalogue by a dedicated pass, exactly like T29/T30/T31 were.

See [README.md](../README.md)'s "Current status" for the short version, `build-orchestration-plan.md` §0 for the full progress guide, and issue [#29](https://github.com/diegoami/imperial_conquest_2/issues/29) for the live tracker.

Read [the roadmap](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/roadmap.md) for the full sequence, [the decompilation plan](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/decompilation-plan.md) for the static-analysis work specifically, [research notes](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/research.md) for the high-level evidence, and the linked reports below before interpreting unknown fields as rules. The latest commit on this repo's `main` as of this handover is `ea56a37`; check `git log` first, this file goes stale fast during an active build.

## Where things are

- `src/IC2.Data/`: C#/.NET 10 parsers for DAT/SAV world prefix, army, nation, recruitment, fleet (`SaveFleetTable.cs`), and mercenary (`SaveMercenaryTable.cs`) tables, and turn state.
- `src/IC2.Inspect/`: CLI inspection — `--inspect-turn/-nation/-city/-army`, `--list-armies <save> <nation>`, `--list-fleets <save>`, `--list-mercenaries <save>`, `--compare-saves`, `--render-map`, `--to-json <save> <output.json>` (dumps everything known about a save to one file). See [README](../README.md).
- `godot/`: Godot 4.7.2 .NET viewer, references `IC2.Data` via `<ProjectReference>` — auto-picks-up `IC2.Data` source edits on next build. Headless check: `"<Godot install>\Godot_..._console.exe" --headless --path godot --quit-after 2`. **Caveat:** this regenerates `godot/project.godot`'s header and flips `godot/MapViewer.cs`'s line endings as a side effect — `git diff -w` before committing after a headless run and revert those two files if the diff is whitespace-only.
- **47 reports** now live in the research repo's `docs/reports/` (see the split notice above), not in this checkout — verified against the repo, which also holds two `*-sha256.txt` checksum files in the same folder that are not reports. 15 are `decompiled-*` (see below); the rest are save-diffing and DAT-table reports. The two newest are `battle-replayed-rout-mechanic-and-combat-constants.md` and `pending-offer-block-army-split-and-naupactus.md`. **Neither is in T04's fixtures corpus yet**, and most of what they add is tactical-reserve research the shipped engine does not consume — but see `build-orchestration-plan.md` T16's hazards for the one existing corpus entry that now needs correcting.
- `docs/design-audit.md`: the audit of `game-design.md` against the evidence base, and the nine open questions it raised. **Read it before acting on `game-design.md`.**
- `docs/build-orchestration-plan.md`: how the 20-milestone backlog is actually executed — 28 tasks with per-task Definition of Done, model and effort, the real dependency graph, the implementer/reviewer/orchestrator pipeline, and the GitHub issue-and-branch conventions. **Read it before starting any build work**; it replaces `game-design.md`'s single-agent `/loop` operating mode with a multi-agent one. Its **§0 is the "where do I check progress, what can I test" reference**; its **§7.5 is the user-initiated pause mechanism** (a label on the tracking issue, checked first thing every tick — no need to find or message the orchestrator directly).
- `docs/release-plan.md`: how the build turns into releases — the SemVer scheme, five version tags anchored to merged task issues, tag/GitHub-Release conventions, the release-note template and who reviews it, and the release checklist. Read it *after* `build-orchestration-plan.md`; it adds no scope, only the version axis.
- `docs/decompilation-plan.md`: **read this first** for static-analysis work — the original 7-item priority queue is fully done, live-updated with what's still open.
- `AGENTS.md`: collaboration instructions (frequent progress updates, 3–4 relevant saves for routine checks).
- User's checkout: `C:\Users\diego\projects\imperial_conquest_2`. Original game files: `C:\Users\diego\Documents\imp_conq_original` (its own git repo), with `saves/` (has a `processed/` subfolder the user moves analyzed saves into), `screenshots/`, `recordings/`, and `notes/1_rome.txt` (the user's action log for the ongoing "1_rome" play session — check for anything newer than last-processed before starting new decompilation work).

## Standing user preferences (from auto-memory — check it's still current)

- **Commit AND push automatically** after finishing a unit of RE work, without asking first. Don't wait for confirmation on either step. Still avoid destructive git operations (force-push, amend, reset --hard) without explicit request.
- When correcting a claim after user feedback (e.g. "battles are never saved," "5,000 is ship capacity not troops," a note typo), fix the actual report text, not just acknowledge in chat.

## Local tooling (all outside the repo, all still present)

- `gh` (GitHub CLI, authenticated as `diegoami`), `jq`, Python 3.14, Node.
- **`ffmpeg`** at `%LOCALAPPDATA%\ReTools\ffmpeg-master-latest-win64-gpl\bin\ffmpeg.exe` — for extracting frames from the recordings in `imp_conq_original\recordings\`. Used as `ffmpeg -ss <startSeconds> -i "<recording.mp4>" -vf fps=1 -frames:v <N> <outdir>/f_%03d.png`, then read the PNGs with the Read tool. The tactical battle UI's combat-resolution panel (`ATTACKS`/`SHOOTS AT` + `UNIT LOSSES`/`TROOP LOSSES`) gives exact before/after troop counts per exchange — this is a much richer source of controlled combat data than manual video scrubbing, and the user is willing to record more battles on request.
- **`%LOCALAPPDATA%\ReTools\`**: Temurin JDK 21 (`jdk-21.0.12.1+1\`), Ghidra 12.1.3 (`ghidra_12.1.3_PUBLIC\`), an imported+analyzed Ghidra project (`ghidra_projects\IC2\`), and `scripts\`:
  - `ExportFunctions.java` — dump all named functions to text.
  - `ExportAddresses.java` — decompile specific addresses; uses `getFunctionContaining()` so it works on mid-function addresses (e.g. string xrefs), not just function starts.
  - `ExportAllInRange.java` — decompile every function (named or not) in an address range to one file. Used to produce `all_app_functions.txt` (0x401000–0x460000, 1,881 functions) — **check this file before re-decompiling anything**, most of this session's later findings came from grepping it rather than fresh Ghidra runs.
  - `FindXrefs.java` / `FindCallers.java` — list references to a data address / callers of a function, via Ghidra's `ReferenceManager`.
  - `FindString.java` — search all of program memory for an ASCII string using Ghidra's own address mapping (more reliable than computing file-offset-to-VA by hand; section alignment padding caused a real off-by-`0x1000` bug once).
  - `DumpMemory.java` — dump raw bytes at an address as little-endian words. **Important finding:** several data tables (unit-type stats, the combat matrix, mercenary names) are uninitialized in the EXE (`MemoryAccessException`) — they're loaded from the DAT file at runtime, so when Ghidra can't read a table, search the DAT file directly by the data's known name strings instead (this worked twice, both times cleanly).
  - `ImportDelphiSymbols.java` — import `delphi_symbols.tsv` into Ghidra as real function symbols.
  - `delphi_symbols.tsv`/`.json` — 282 recovered real method names across 31 classes. **Look up a method's address here by class name before hand-deriving one** — most classes relevant to game rules are already named.

**A real Ghidra limitation found and worked around:** `ReferenceManager.getReferencesTo()` finds *zero* references to several known-real strings in this binary (tested: the DAT filename, `"falls to"`) — a genuine gap in Ghidra's default analysis of this Delphi build's string-loading pattern, not a dead end in the data. **Workaround that works every time:** decompile the whole application range to one file (`ExportAllInRange.java`) and grep the *decompiled text* — Ghidra's decompiler inlines string-literal operands into pseudocode even when no formal Reference was ever recorded. Prefer this over `FindXrefs.java` for any string not already tied to a known address. Function-to-function call xrefs (`FindCallers.java`) work fine, except across **virtual method calls** (e.g. `TStream.Read`), which are a real dead end for backward call-graph tracing — go find the caller by name/string instead.

To resume: `$env:JAVA_HOME = "...\jdk-21.0.12.1+1"`, then `ghidra_12.1.3_PUBLIC\support\analyzeHeadless.bat <projectDir> IC2 -process "Imperial Conquest 2.exe" -noanalysis -scriptPath <scriptsDir> -postScript <Script>.java <args...>`.

## Confirmed findings — the big picture

### Save/DAT structure (essentially complete now)
The **entire SAV file layout is confirmed directly from the actual read/write code** ([`decompiled-sav-file-layout.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-sav-file-layout.md)), not just inferred: map (89,600B) → city table (11,356B = 334×34) → army count + records (656B each) → fleet count + records (26B each) → nation table (16×1,172B) → **mercenary table, exactly 50×12B, hard-coded in code** → **news log, a 40-slot ring buffer of 61-byte messages** ([`decompiled-news-log-identified.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-news-log-identified.md)) → a handful of small fixed fields including the **16-entry nation turn-order table** and a **pending-diplomacy-offer indicator** ([`decompiled-turn-and-calendar-sequencing.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-turn-and-calendar-sequencing.md)) → the known 55-byte trailer. The pending-offer block is now **decoded and confirmed against real saves**: 4 bytes at `fileLength − 22`, `{short proposingNationIndex (0xFFFF = none), short proposedRelationState}`, the second word reusing the relation-matrix encoding (`1` = trade) rather than a bespoke enum — two consecutive saves whose news read "Greece wants to trade with Rome" and "Bythinia wants to trade with Rome" read `07 00 01 00` and `0B 00 01 00`, with the saves either side reading `FF FF`. One offer game-wide, not one per nation. A battle-in-progress branch exists in the code but **the user confirmed saving mid-battle isn't possible in the game** — every real save's length is fully accounted for without it.

Map markers `333`/`335` = fleet positions (exact coordinate match). Fleet `CityIndex` is **not a fixed home port** — it changes as a fleet acts, candidate meaning "last city interacted with." Fleet **`OwnerCode` (+8) is now confirmed** — two independently-identified fleets (Rome via a controlled order, Carthage via a news-log-correlated loss), both matching exactly, plus all 4 in-port fleets in the same save matching their port's owner. See [`fleet-owner-field-confirmed.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/fleet-owner-field-confirmed.md).

### City capture, defection, and combat ([`decompiled-city-capture-resolution.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-city-capture-resolution.md), [`decompiled-defection-and-siege-attrition.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-defection-and-siege-attrition.md), [`decompiled-combat-formula-structure.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-combat-formula-structure.md), [`combat-type-effectiveness-matrix.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/combat-type-effectiveness-matrix.md))
- Siege outcome = attacker strength (archers tripled) vs. defender strength (fortification/loyalty-based, −20% if owner≠allegiance).
- Capture transfers unity (+9/−15), wealth, city count, and the exact `nationTaxBase` field (solved to 2,440 for Rome from the tax formula) between nations.
- **A cascading defection mechanic**: after a capture, nearby weakly-defended low-loyalty cities of the same defeated nation can auto-defect — very plausibly the mechanism behind every "X defects to Y" news event this project has ever seen.
- Defection (unlike forced capture) never touches population/fortification — exact match to the empirical Modena finding.
- Population/fortification loss traces to two functions run on *every* siege attempt (win or lose): random army casualties, and a smoothing/decay adjustment to city-stat fields — meaning a capture's visible loss may be accumulated across multiple attempts, not a one-time penalty.
- Combat: melee hits both sides simultaneously per exchange, scaled by a unit-type effectiveness ratio (the 5×5 matrix is located in the DAT file, right after the unit-type table), capped at 30,000 and 40% of own troops. Shooting has a real range mechanic (in-range shots double). A "focus-fire" counter scales losses when multiple attackers target one defender.
- **The 40% melee-loss cap is now confirmed exactly** (`floor(0.4 × defenderTroops) + 1`, matching 4 of 5 fresh recorded exchanges precisely) — see [`battle-recording-melee-cap-confirmed.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/battle-recording-melee-cap-confirmed.md). **Both follow-ups are now settled**: the cap is confirmed on the **attacker** side too (a light-infantry unit of 7,135 losing exactly `⌊0.4 × 7,135⌋ + 1 = 2,855` to heavy infantry), plus five more exact defender-side hits, and the effectiveness matrix's attacker/defender axis was resolved from the decompiled index arithmetic rather than from a closer fight — see [`battle-replayed-rout-mechanic-and-combat-constants.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/battle-replayed-rout-mechanic-and-combat-constants.md). A closer fight is still the only way to exercise the un-capped formula branch.

### Economy ([`decompiled-fleet-tax-and-mercenary-formulas.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-fleet-tax-and-mercenary-formulas.md), [`decompiled-recruitment-cost-formula.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-recruitment-cost-formula.md), [`unit-type-stat-table-in-dat.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/unit-type-stat-table-in-dat.md), [`decompiled-quarterly-billing-and-economy.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-quarterly-billing-and-economy.md))
- Fleet order: `cost = ships×10`, `capacity = ships×500`, `upkeep = ships×3/quarter` — all three now confirmed exactly.
- Recruitment/upkeep: `cost = (troops/200) × priceTable[unitType]`, same table for initial recruitment and ongoing quarterly upkeep (army and garrison alike). Unpaid armies lose troops for real, not just debt.
- Tax income: `income = nationTaxBase × tax% / 100`, `nationTaxBase = 2,440` solved exactly for Rome, and confirmed as the literal quarterly treasury credit (not just a dialog preview).
- **"Quarterly" = once per season** (4×/year) — found by locating the function called only at the week-11→1 wrap.
- The full unit-type stat table (moves, battalion size, shots, range, recruit cost) is in the DAT file, every field cross-checked against real observations.
- Tribute grows toward a population target moderated by tax rate; loyalty responds to tax rate with a rebellion check below a threshold; unity decays 3/quarter; diplomatic relations drift toward peace over time.

### Turn/calendar ([`decompiled-turn-and-calendar-sequencing.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-turn-and-calendar-sequencing.md), [`decompiled-weather-events.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-weather-events.md))
Week `+2 mod 12`, season advances at 11→1, year decrements at Winter→Spring — this closes the project's original oldest open item. City-unit `StateCode` increments 2/week capped at 24 (24 is a cap, not a starting value). Fleet construction countdown and completion confirmed. Population growth is seasonal/loyalty-modulated with a Winter decline chance. Army supply consumption is seasonal. A previously-unknown **weather-event system** exists (~8× more frequent in Winter), very plausibly the source of a fleet storm/loss-at-sea mechanic.

### The design audit (most recent work — read `docs/design-audit.md` first)

`docs/game-design.md` got the movement rule wrong twice in a row (first from a generic 4X assumption, then from an over-correction), so the whole design was audited against the evidence base: every one of the 31 classes / 282 methods in `delphi_symbols.tsv` was swept for mechanics no report had touched, every tagged claim re-opened against its cited report, and the milestone backlog reviewed. `docs/design-audit.md` is the deliverable, in four parts (missing mechanics / claim audit / **nine open questions for you** / build-plan review). Three new reports carry the evidence: [terrain-move-cost-table-in-dat.md](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/terrain-move-cost-table-in-dat.md), [decompiled-unit-map-orders-and-record-fields.md](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-unit-map-orders-and-record-fields.md), [decompiled-diplomacy-peace-terms-and-instant-battles.md](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-diplomacy-peace-terms-and-instant-battles.md).

Headlines:

- **The terrain/move-cost table is located** (DAT `0x1F622`): `Sea` 1 · `Sea` 3 · `Plain` 1 · `Desert` 1 · `Forest` 2 · `Mountains` 4 · six `River` codes at 4. **All land terrain costs moves** — the "rivers only" conclusion was wrong, and the moves-zeroing rule is AI-only.
- **Diplomacy is not a dead end.** Relation matrix (nation `+0x26`), 4 states plus negative cooldowns (−8/−24/−18), 3-trade-partner cap, alliance/war contagion, attacking auto-declares war, post-battle peace terms, and **`reparations = W/4 + random(W/4) + cityCount × 10`**.
- **The original has its own instant battle resolver** for AI-vs-AI fights (power comparison, loser annihilated), plus a third resolver for naval battles. Both were unknown.
- **Whole subsystems the design is silent on**: naval transport (1 army/fleet, 500 troops/ship) and naval combat, fleet condition/repair/scuttle, supply as a *purchased* economy (1 talent per 5 tons, army/fleet money purses capped at 1,000, capacity `troops/100` and `ships×8`), city fortification orders, army/unit join-split-rename.
- **Victory condition**: holding all 334 cities (`THumanFalls_InitializeForm`); 270 BC start, candidate 250 BC end.
- **Confirmed absent**: no leader/general system, no technology or research, no espionage, no city improvements beyond fortification.
- **Record-field corrections, now applied to `IC2.Data`**: `ArmyRecord +8` is the covered map cell (not morale), `+14` is morale, and unit slot `+0` is the regular/mercenary marker; `FleetRecord +20` is the fleet's condition % once launched (only a build-city index while under construction), and `+10`/`+12`/`+14`/`+16`/`+22`/`+24` are now labelled too. The corrected parser was re-validated against the sample saves and the results are decisive — see part 4 of [`decompiled-unit-map-orders-and-record-fields.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-unit-map-orders-and-record-fields.md) (91 mercenary-flagged units across three saves, every one with an ethnic name and every regular with an auto-generated battalion name; Army 2's computed 65% matching the original's own panel; the four `(0,0)` fleets all reading as under construction with 0 supply/0 money).

## What's still open

- **Eight of the nine** open questions in `docs/design-audit.md` §3 are now answered and folded into `game-design.md`. Q1 (original's instant resolver) and Q2 (naval in scope) were answered first. Q3 (diplomacy fidelity), Q4 (per-army purses), Q5 (default victory condition), Q6 (human/AI asymmetries) and Q8 (bug-reproduction policy) are answered together, the same way: ship **both** as two named, user-selectable rulesets, `classical-faithful` and `improved`, surfaced as a prominent choice at New Game (`game-design.md` "Two shipped presets"). Q7 (city-orders extensibility) is answered independently (a generic `cityOrders` table). A new mechanic joined the same table: `combat.onDefeat` — `improved` has a defeated field/naval army scatter a few tiles away instead of being destroyed, `classical-faithful` keeps the original's annihilation. The milestone list and `build-orchestration-plan.md`'s task catalogue (T08/T09/T12/T14/T15/T16/T19/T24) have been revised accordingly. **Only Q9 is still open** — a real evidence gap, not a decision, and it doesn't block starting milestone 1.
- One evidence gap worth a controlled save: the code says buying supply costs `amount/5` from the army's money purse, but the frames in [`galatia-elimination-and-city-resupply-confirmed.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/galatia-elimination-and-city-resupply-confirmed.md) show army money unchanged.
- The reparation formula has not been checked against the one observed payment's actual field values (a pure save-read, no new play session needed).
- `FUN_0044A98C` (defender siege strength) — the −20%-if-owner≠allegiance claim in [`decompiled-city-capture-resolution.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-city-capture-resolution.md) and the ×9/10-if-attacker==allegiance branch in `FUN_0044B27C` need reconciling. **Still unreconciled, but no longer invisible**: T02 ships both as separately-named `ruleset.siege` flags with the conflict written into their `_provenance`, so T17 inherits a visible decision rather than a silently-picked winner. Resolving it needs new decompilation work, not a user decision.
- ~~One unidentified unit-type-table field remains (`+0x20`)~~ — **closed**: `+0x20` is the per-type **shooting vulnerability**, read for the *target's* type by `FUN_0043845c` (light inf 18 · heavy inf 2 · archers 18 · light cav 15 · heavy cav 4), checked against 23 recorded shooting exchanges. `+0x26` was already solved (per-type combat-power weight). **Every field in that table is now accounted for.** `+0x1A` also gained a second use: `standardBattalionSize / 25` is the tactical rout threshold.
- The rebellion check (`FUN_0044c204`) and weather-event effect (`FUN_004511bc`) internals — not decompiled.
- The un-capped melee formula's exact random-roll term — **still open**; the cap itself is now confirmed exactly on **both** sides (eleven observations, two recordings). ~~The effectiveness matrix's attacker/defender axis~~ — **closed**, settled from the melee function's index arithmetic (10-byte row stride on the attacker's type, 2-byte column stride on the defender's), confirming the orientation already published; no closer fight was needed after all. A full simulation of a multi-round recorded battle is still open and is now **better specified**: the same save fought twice gives 63,282 and 75,536, so the tactical resolver is strongly stochastic and any simulation must be validated as a *distribution* test over many seeds, never an equality test.
- **New, and the biggest combat finding since the matrix**: a **rout mechanic** (`FUN_00438fb0`) is the only way a unit ever reaches zero troops — the 40% caps can only asymptote toward 1. Removal below `standardBattalionSize / 25`, at tactical morale ≤ 19, or on a `Random(m)+Random(m) ≤ 29` check in the 20…39 band, plus a cascade (−6 morale to every friendly unit, recursively pulling out anyone below 30; +5 to every enemy). Reserve research only — the shipped instant resolver does not need it (`game-design.md` §Combat, `build-orchestration-plan.md` T16 hazards). Still unobserved: the cascade itself, and whether the morale branch ever fired in the two recorded battles.
- The tactical post-battle **promotion rule is now a single uniform 1-in-4 roll** matching the instant resolver's own; the previously-derived slot-adjacency rule is **withdrawn** (it failed three ways on a replay of the same battle). The implementing code is still not located, so this remains empirical over 30 units in two battles.
- A ~6-byte reconciliation gap in the SAV layout's news-log region sizing.
- **The DAT's static tables are located but mostly unidentified** — fourteen small tables totalling 1,494 bytes at `0x1F2F0` (only the season table at `0x1F7D8` is identified), plus four larger ones (1,040 / 3,012 / 4,992 / 2,440) of which only the leader-name pool is. See [`dat-file-layout.md`](investigations/dat-file-layout.md); several are plausibly the unit-type and terrain tables the fixtures corpus already carries from other reports. Not a blocker for T29 or T30.
- **Storm-attrition predicates**: `FUN_004494e4` (the away-from-friendly-coast doubling) and `FleetRecord +24 == 1` (the tripling) are inferred from magnitudes in the Cartago series, not decompiled — T14 carries them as `[derived]` ruleset values.
- `ComputerGeneral`'s actual AI decision-making (only its dispatch chain was traced; the roadmap's own scope decision favors faithful *rules* over a byte-exact AI port anyway).

## The battle-freeze investigation — **CLOSED** (kept as project history)

> **Resolved, September 2026. It was never a freeze, and it was never a compatibility problem.** The mechanism is `FUN_00448ffc` at `0x00448FFC` — `Delay(n)`, a `GetTickCount` busy-wait spinning for `n × 100` ms — called once per resolved combat exchange (`FUN_0043910c` shooting, `FUN_004393ec` melee). Because it busy-waits instead of sleeping it never returns to the message pump, which is exactly why the window said "Not Responding" *and* why Procmon saw perfect silence rather than a blocking syscall. Both of that report's original open items are now closed by the same finding, and the answer to "is it skippable via an in-game setting?" is **yes**: `n` is not a constant but a **stored per-nation player preference**, read from `nation[+0x468]` (shooting) and `nation[+0x466]` (melee), with its own settings dialog in the recovered symbol table (`TBattleDelays_InitializeForm` / `_ChangeDelay` / `_OK` at `0x004367e0`…). Setting both to 0 makes battles run at full speed, with no patching of any kind. It was adjustable from inside the game's own options the entire time. The user separately reached the same address by patching (`patch_exe.py` in the local game directory builds an instant-battle variant and a message-pumping variant, and flips `PlaySoundA` at `0x45C013` from `SND_SYNC` to `SND_ASYNC`); the message-pumping build is what made the first fully legible per-exchange battle recording possible, and hence the rout mechanic. See the update section in [`battle-freeze-diagnosed-procmon.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/battle-freeze-diagnosed-procmon.md).
>
> **Practical consequences**: battle recordings are now cheap — no more "expect ~3 s of dead wait per exchange, prefer lopsided matchups", the constraint that shaped every recording request this project has made. Nothing about this reopens a design decision: the shipped resolver is the instant one and has no per-action step to pace. For a reimplementation the only carry-over is that pacing is a **UI settings value**, not a combat rule (`game-design.md` §Combat records this).

The history below is kept because the dead ends are worth not repeating — the Win98-VM item especially, which remains ruled out on its own separate grounds and should not be re-suggested. The game reliably froze/became unresponsive during manual tactical battles. What was tried:

- **Procmon diagnosis**: found ~3.02-second dead-silence stalls (zero syscalls) recurring throughout a battle, each right after a short sound-effect playback thread exits. Consistent with either a hardcoded pacing delay or a blocking wait — Procmon can't distinguish a benign `Sleep` from a CPU-spinning busy-wait, since both look identical (silence) from a syscall trace.
- **WAV format conversion tried, did not fix it**: the game's `WAVS` files were legacy 8-bit unsigned PCM at 5512/11025/22050 Hz; converted all to 16-bit/44.1kHz PCM (originals preserved in `WAVS - Copy`) in case the odd format was forcing an expensive converter-chain rebuild in the modern audio engine. No improvement.
- **A Windows 98 SE VM was attempted (VirtualBox) and specifically ruled out — do not re-suggest this without the underlying condition changing.** Two Windows 98 installs both hit `SHELL32.DLL is linked to missing export SHLWAPI.DLL` crashes, even from a completely fresh disk with VirtualBox's own hardware-virtualization setting already disabled. Root cause confirmed directly in `VBox.log`: `HM: HMR3Init: Attempting fall back to NEM: VT-x is not available` — the host's WSL2 install requires Windows' Virtual Machine Platform (Hyper-V-based), which exclusively claims VT-x, forcing VirtualBox onto Microsoft's Windows Hypervisor Platform (WHP) backend regardless of any per-VM setting. WHP's real-mode/V86 CPU emulation is known to be less accurate than VirtualBox's native engine, which plausibly explains both crashes. The real fix (`bcdedit /set hypervisorlaunchtype off` + reboot, disabling WSL2 until reverted) was declined by the user — **they explicitly do not want WSL2 disabled**, so a Win9x-native VM is off the table on this machine. The VM (`ImpConq2-Win98`) has been fully removed (unregistered + disk deleted).
- **Net effect at the time**: the freeze had to be characterized from modern-Windows data only (Procmon traces, video recordings, in-game battle-resolution screens) — there was no clean native-OS reference environment to compare against on this machine. **That constraint turned out not to matter**: the answer came from the decompiled EXE, not from a reference environment, and no native-OS comparison was ever needed. The Win98-VM item above stays ruled out regardless (the WSL2/VT-x conflict is unchanged and the user still does not want WSL2 disabled) — it simply is no longer a gap worth caring about.

## Build and verification

```text
dotnet build src/IC2.Data/IC2.Data.csproj
dotnet build src/IC2.Inspect/IC2.Inspect.csproj
dotnet run --no-build --project src/IC2.Inspect/IC2.Inspect.csproj -- --to-json saves/<any.sav> out.json
```
Build `IC2.Data` before `IC2.Inspect` if both changed (shared `obj` dir). All builds clean as of `71975b1`.

## Next useful work

1. **T31 is `status:ready` and is the single thing to dispatch next, alone.** It corrects three swapped `Ruleset.Siege` defender-strength field weights inherited from T02, is the only thing blocking T07 (suspended, `status:blocked`, since [PR #44](https://github.com/diegoami/imperial_conquest_2/pull/44)'s review found the defect), and sits at the front of the critical path (`T02 → T31 → T07`). Full spec: `build-orchestration-plan.md` → T31; evidence: `docs/investigations/siege-defender-strength.md`. Once T31 merges, resume T07 (rebase onto T31's field names, do **not** re-implement `Ruleset.Siege` from inside T07's own Owns list).

2. **Two bugs are filed but not yet triaged into the plan**: [#46](https://github.com/diegoami/imperial_conquest_2/issues/46) (a misnamed threshold constant) and [#47](https://github.com/diegoami/imperial_conquest_2/issues/47) (a wrong-shape penalty formula that **T17 will inherit** if not fixed first). Per §6.7's bug-list mechanism, these need a dedicated planner pass (not a routine tick) to decide: fold into an upcoming task's DoD, or spin up their own correction task. #47 is the more urgent of the two since T17 depends on it.

3. **Then the rest of wave 3.** T07 (once resumed), T08 economy, T09 movement, T10 news log, T11 asset pack, T12 victory — all dispatchable now (T08/T12 needed T06, merged; T11 needs only T02). T07/T08/T14 carry DoD lines for the confirmed supply→morale and fleet-attrition mechanics (`docs/investigations/thracia-supply-morale.md`). Exactly one code-modifying agent at a time (§9) — these run serially.

   **Not one of the five Phase 0 tasks passed review first time**, and every rework round caught a real defect, so the review gate is confirmed working rather than process theater. Four worth remembering, because each was invisible to the task that shipped it: T02 improvised the starting calendar week in C# with the wrong parity, so the season would never have advanced and **T06's calendar DoD could not have passed** — no test caught it because the fixture overwrote the calendar by hand. T04 declared `Fixtures` inside `namespace IC2.Engine.Tests.Fixtures`, so `FixtureCorpus.Get(...)` — the call §2.4 promises every later task will make — failed with CS0234 from any sibling namespace, and would have hit ~14 downstream tasks. T03 shipped a hand-written SplitMix64 whose tests all asserted *relative* properties, so **corrupting the generator (`z >> 30` → `z >> 29`) left all 121 tests green** — every wave-3 golden fixture would have baked in the corruption permanently; it is now pinned to the published reference vector. And T03's determinism guard missed `foreach (var (k, v) in dict)`, the most idiomatic dictionary iteration there is. T04's *data*, by contrast, survived two independent re-derivation passes plus a `/code-review` pass with **zero wrong values across 355 entries**. **To check progress or see what's testable at any stage, read `build-orchestration-plan.md` §0 first** — issue #29 is the live dashboard. **To pause for any reason, no justification needed, see §7.5.**
4. **Keep README.md and HANDOVER.md current after every merge** — `build-orchestration-plan.md` §2.3 assigns this to the orchestrator. It was skipped for wave 0 and had to be fixed by hand afterward; it has been honoured in the same tick as every merge since, and §7.3 now states outright that a merge without the matching doc update is an incomplete tick. If dispatching further waves, still confirm the orchestrator's prompt includes this step rather than assuming the plan document alone is enough.
5. **Q9 has a drafted answer sitting unmerged**: `notes/q9-supply-purchase-cost` branch (pushed, not merged as of this writing) answers it directly from the user's own play experience — free at your own cities, costs money elsewhere — reconciling the code's `amount/5` debit against the Galatia report's unchanged-money frames (that example was a free own-city resupply, not a paid purchase). Needs a merge decision, not new research.
6. **[The release plan](release-plan.md)** is now written — the SemVer scheme, the five release tags (`v0.1.0` … `v1.0.0`) and the merged-task gate for each, who cuts and publishes a tag, the release-note template and its eight required sections, and a 19-line release checklist. Tags land at **capability jumps, not phase boundaries**; each of the 28 task issues now also carries a `release:*` label naming the first release it gates, so a gate is a `gh issue list` query rather than a hand-kept list.
7. **`docs/game-design.md`** is the reference for that build — data-driven rulesets, arbitrary custom maps, the original's instant battle resolution ported (no tactical grid, no pacing-freeze risk), full naval, local hotseat, a scenario system, and asset packs. Read `docs/design-audit.md` alongside it; several of its sections are corrections of earlier, unevidenced claims.
8. **New save/screenshot/note cycles still take priority if one shows up** — check `imp_conq_original/notes/` for anything newer than the last-processed save; `2_rome_s.txt` and `3_rome.txt` **have now been processed** through the `/process-evidence` skill ([`evidence-pipeline.md`](evidence-pipeline.md)) — research-repo commits `f754d5a` and `dd49b3e`, two new reports (`battle-replayed-rout-mechanic-and-combat-constants.md`, `pending-offer-block-army-split-and-naupactus.md`) and eight existing ones updated; the design-side follow-through is the `findings/rout-mechanic-and-freeze-closure` branch in this repo. Check `notes/` for anything newer than those two.
8. Pick up any "What's still open" item above, per `docs/decompilation-plan.md`'s live status, if `game-design.md` isn't the priority.

Keep original EXE, DAT, HLP, CNT, WAV, SAV, screenshots, and recordings outside the public repository — this also applies to the Ghidra project and decompiled-text dumps under `%LOCALAPPDATA%\ReTools`. Record exact file/hash/address/screenshot evidence for each new field or formula and mark inferred meanings as candidates until checked.
