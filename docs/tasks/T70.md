#### T70 Command-layer hygiene: one distance metric, one rejection file, and the naming filter

- **Design milestone**: none — follow-ups to T15, T46, T50 and T55, bugs [#189](https://github.com/diegoami/imperial_conquest_2/issues/189), [#190](https://github.com/diegoami/imperial_conquest_2/issues/190), [#212](https://github.com/diegoami/imperial_conquest_2/issues/212) (N1) and [#245](https://github.com/diegoami/imperial_conquest_2/issues/245) (N1). **Labels**: `phase:2 lane:engine`
- **Branch**: `task/T70-command-hygiene` · **Model/effort**: Sonnet / **Low** · **Reviewer**: **Opus / Medium**
- **Start after**: — · **Merge after**: —
- **Owns**:
  - `src/IC2.Engine/Naval/Commands/BuyFleetSupplyCommand.cs` (the citation only), `BuyFleetSupplyCommandHandler.cs`, `RepairFleetCommandHandler.cs` and `ScuttleFleetCommandHandler.cs` (the distance lines only)
  - `src/IC2.Engine/Naval/FleetTickSystem.cs` — **the distance line only** (~:202)
  - `src/IC2.Engine/Economy/Commands/BuySupplyCommandHandler.cs` — the distance lines only (~:101, ~:185)
  - `src/IC2.Engine/Recruitment/Commands/HireMercenaryCommand.cs` and `HireMercenaryCommandHandler.cs` — the rejection codes only
  - `src/IC2.Engine/Armies/ArmyNaming.cs` — `HighestOrdinal` and its remarks only
  - their tests: `tests/IC2.Engine.Tests/Naval/FleetSupplyEndToEndTests.cs`, `tests/IC2.Engine.Tests/Economy/Commands/BuySupplyCommandHandlerTests.cs`, `tests/IC2.Engine.Tests/Recruitment/HireMercenaryCommandHandlerTests.cs`, `tests/IC2.Engine.Tests/Armies/ArmyNamingTests.cs`, and the naval command tests only if a distance change needs them
  - `src/IC2.Engine/Naval/FleetAttritionRule.cs`: **only the N-4 remark at ~:250–262** (folded from [#300](https://github.com/diegoami/imperial_conquest_2/issues/300) NB-1, 2026-09-24). No code line.
  - **Granted 2026-09-24 by the user, narrowly, to fold in bug [#345](https://github.com/diegoami/imperial_conquest_2/issues/345) from the v0.3.0 milestone review:** `src/IC2.Engine/Naval/Commands/DisembarkArmyCommandHandler.cs` (the distance line only); `src/IC2.Engine/Model/Ruleset.cs` and `GameDataSchema.cs` (**one** new field, the adjacency radius); `data/rulesets/*.json` (that key and its `_provenance` only; `classical-faithful.json` regenerated through the exporter); `tests/fixtures/corpus.json` (its id, only if none exists).
  - **Not** `Battle/**` or `Cities/Capture/**`: their Chebyshev copies are T66 Done-when 6 and T67 Done-when 6.
- **Scope**: Small, merged-code tidies that no ready task owns.

  - **#189.** `BuyFleetSupplyCommand.cs:23–24` cites `decompiled-unit-map-orders-and-record-fields.md` for the provider rule, *"a city within one tile whose owner is not at war with the buyer"*. That report does not carry it. [`supply-capacity-rounding.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/supply-capacity-rounding.md):33 carries both halves (checked 2026-09-23 at research `3f6ca09`): *"every city within one tile whose owner is not at war (relation `3`)"*.
  - **#190** (T50's review).
    - N3: `FleetRunToTheEdgeOfStarvation_UnsuppliedControlSinks_WhileResuppliedSurvives` (`FleetSupplyEndToEndTests.cs` ~:123) survives on a 2-point margin: condition 42 against a threshold of 40 under a mutation that starves every fleet.
    - N5: two migrated tests park the army **on** `meridia`'s tile, where distance 1 is the natural fixture. The comment on #190 refutes N5's siege reasoning (an army never occupies a city tile; a siege is issued from adjacency), but the move to distance 1 stands on its own.
    - N6: three seams clamp the purse unguarded and one guards on `EconomyPurses`. Recorded as known; no action.
    - N7: Chebyshev distance is spelled many ways, while `LandingTile.ChebyshevDistance` (`Naval/LandingTile.cs:56`) exists.
  - **#212 N1.** The mercenary hire's rejection codes live in two files: `HireMercenaryRejections` (`HireMercenaryCommand.cs:34`) and `HireMercenaryUnitCapRejections.OverArmyUnitCap` (`HireMercenaryCommandHandler.cs:19–27`). N2 is PR-body text and needs no action.
  - **#245 N1.** `ArmyNaming.HighestOrdinal` (~:201) lacks the original's `troops > 0` filter. Its own remarks (~:188–198) record the case: a zero-troop slot with a stale battalion name is counted where the original skips it. #245 N2 is process text, carried on the triage plan branch.
- **Done when**:
  1. **The citation (#189).** `BuyFleetSupplyCommand.cs`'s provider rule cites `supply-capacity-rounding.md`, and the PR quotes the line that carries both halves. No other file in `Naval/Commands/` or `Economy/Commands/` cites `decompiled-unit-map-orders-and-record-fields.md` for the provider rule.
  2. **A real margin (#190 N3).** The test keeps resupplying the surviving fleet for the whole run, or asserts a minimum condition gap between the two fleets. The mutation that applies the starvation penalty to every fleet regardless of supply fails it; show that in the PR.
  3. **Distance 1 (#190 N5).** `Buying_at_a_foreign_city_costs_amount_divided_by_five` (~:202, army placed at ~:217) and `A_foreign_purchase_the_purse_cannot_afford_clamps_to_zero_instead_of_rejecting_it` (~:244, ~:255) place the army at Chebyshev distance 1 from `meridia`, not on its tile, and both still pass.
  4. **One distance metric (#190 N7).**
     - `BuySupplyCommandHandler.cs` ~:101 and ~:185, `RepairFleetCommandHandler.cs` ~:64, `ScuttleFleetCommandHandler.cs` ~:54 and `FleetTickSystem.cs` ~:202 call `LandingTile.ChebyshevDistance`.
     - `BuyFleetSupplyCommandHandler`'s private copy (~:175–176) is deleted.
     - Afterwards, `git grep -n -E 'Math\.Max\(\s*Math\.Abs' -- src` lists only lines under `Battle/**` and `Cities/Capture/**` until T66 and T67 merge.
     - `Economy/HostileArmyAdjacent.cs` ~:37's per-axis radius test is equivalent, outside Owns, and left; the PR lists it as known.
  5. **One rejection file (#212 N1).** `OverArmyUnitCap` moves beside `HireMercenaryRejections` in `HireMercenaryCommand.cs`, with its code string unchanged, and the handler file declares no rejection class.
  6. **The naming filter (#245 N1).** `HighestOrdinal` skips a unit with `troops <= 0`, matching the original. A test gives an army a zero-troop slot whose name carries a higher ordinal than any live unit and shows the next name ignores it. The remark at ~:188–198 says the filter now exists.
  6a. **Folded follow-up ([#300](https://github.com/diegoami/imperial_conquest_2/issues/300) NB-1, added 2026-09-24).** `FleetAttritionRule.cs`'s N-4 remark (~:250–262) points at the death check where it really is (about 25 lines below, not "two lines below"), and describes Decision 6 as what it covers (one draw per unit instead of 20 per pass), not as disclaiming "exactly this kind of case". Text only; `git diff` of that file shows only comment lines.
  6b. **Folded bug ([#345](https://github.com/diegoami/imperial_conquest_2/issues/345), added 2026-09-24): the radius is ruleset data.** The one distance helper Done-when 4 introduces compares against a ruleset value (the adjacency radius, 1 in both presets), not a `> 1` literal, at every site this task owns plus `DisembarkArmyCommandHandler`. Its `_provenance` cites the original's distance-1 rule ([`decompiled-mobilization-and-mercenary-restock.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-mobilization-and-mercenary-restock.md) ~:143–144, `d == 1` with Chebyshev distance). A test sets the radius to 2 in a test ruleset and shows a purchase at distance 2 is then accepted. No shipped value changes.
  7. `dotnet build IC2.sln` and `dotnet test IC2.sln` green, and the diff lists only Owns paths. No seeded outcome and no golden transcript moves.
- **Hazards**:
  - **A pure refactor except Done-when 6.** `LandingTile.ChebyshevDistance` takes `GridPoint`s. Every replaced line must compute the same value, and the CLI golden transcript is the check.
  - **T63 edited `FleetTickSystem.cs`** (the storm call, merged as `562e608`); this task changes one distance line there, at ~:202 on `7e18d83`.
  - **T56 owns the restock system in `Recruitment/**`.** This task touches only the two hire-command files, and they are disjoint from it.
  - **#190 N6 is recorded, not changed.** The asymmetry is known.

---
