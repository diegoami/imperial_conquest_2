#### T69 What elimination does to diplomacy, and a diplomacy tidy

- **Design milestone**: none — a correction to merged T19 code, bug [#199](https://github.com/diegoami/imperial_conquest_2/issues/199) with the user's decision of 2026-09-23, plus [#200](https://github.com/diegoami/imperial_conquest_2/issues/200) and [#254](https://github.com/diegoami/imperial_conquest_2/issues/254). **Labels**: `phase:2 lane:engine`
- **Branch**: `task/T69-elimination-diplomacy` · **Model/effort**: Sonnet / High · **Reviewer**: **Opus / Medium**
- **Start after**: T63, T68 · **Merge after**: T63, T68 (T68 makes the exporter own preset-specific provenance, and this task deletes a ruleset key from all three presets, `classical-faithful.json` through the exporter)
- **Owns**:
  - `src/IC2.Engine/Diplomacy/**`, `tests/IC2.Engine.Tests/Diplomacy/**`
  - `src/IC2.Engine/Model/Ruleset.cs` — **deleting `RulesetFlags.BugPolicyDiplomaticThaw` and its `DiplomaticThawPolicy` enum only**, plus the two remarks that name it (`FaithfulThawColumnBug`'s at ~:1237 and `BugPolicySiegeRatioClamp`'s at ~:1257)
  - `data/rulesets/*.json` — **that key and its `_provenance` only**, plus the two `improved.json` provenance strings that name it (`faithfulThawColumnBug`, `bugPolicySiegeRatioClamp`); `classical-faithful.json` regenerated through the exporter
  - **these lines only**: `tests/IC2.Engine.Tests/Export/ExportedRulesetTests.cs` ~:97, `tests/IC2.Engine.Tests/Model/ModelCoverageTests.cs` ~:274, and `tests/IC2.Engine.Tests/Presets/ImprovedPresetTests.cs` ~:65–66, ~:162 and ~:177
- **Scope**:

  - **#199: no diplomacy handler rejects an eliminated counterparty.** `ProposeTradeCommandHandler`, `ProposeAllianceCommandHandler` and `AcceptPendingOfferCommandHandler` never check `NationState.Eliminated` on the other side; `CommandDispatcher` checks only the issuer. A nation eliminated between the turn-start offer roll and the acceptance still gets a `Trade` relation written to it, and T17 makes that path live. **The user decided on 2026-09-23** that:
    - relations with a nation that is later eliminated are **kept, as history**, not broken;
    - every propose and accept handler **rejects** an eliminated counterparty, with **one shared typed rejection**, the way T19 factored `RelationTransitions.IsAtWarWithAnyone`.

    This is `[designed]` unless the research finds the original's own cleanup rule. T17's review (N7) says the original also cleans diplomatic and mercenary state on elimination, so the task **first checks the research repository** and says what it searched.
  - **#200** (T19's reviews). N4: `RulesetFlags.BugPolicyDiplomaticThaw` (`Ruleset.cs` ~:1263) is read by no `src` code and duplicates T19's live `FaithfulThawColumnBug`: two flags for one behaviour, one dead. R2-F1: `HonourablePeaceGateDivergenceTests`' class remark (~:14–15) calls 400 a non-multiple of 100. R2-F2: `B1_ConfirmedForm_FiresWhenTheDividedScoreActuallyIsLower`'s summary (~:54–58) belongs to a different test. R2-F4: `AcceptPendingOfferRejections.UnknownProposer` (`AcceptPendingOfferCommand.cs:40`) has no test. N2 (T19 Done-when 5, re-verify once an attack command exists) was answered by T54's two-command sequence, which #222 records; N3, R2-F3, R2-F5 and R2-F6 are informational.
  - **#254** (T36's review). `ImprovedPresetTests.CollectDifferingPaths` opens with a `pathPrefix.EndsWith("._provenance")` guard (~:177) that can never fire, because the parent loop already skips that key.
- **Done when**:
  1. **Research first (#199).** The PR's first section says what was searched in the research repository for the original's elimination handling (which reports, which functions) and what it says about relations with, and offers to or from, an eliminated nation. If the original's rule is found **and differs from the user's decision, stop and report** before writing code: the decision goes back to the user. Otherwise the code remarks tag the rule `[designed]` and state that search.
  2. **One shared rejection.** A single typed rejection for an eliminated counterparty, defined once, is used by the three handlers. Each rejects before any state change; one test per handler.
  3. **The cross-task scenario.** A nation is eliminated mid-turn through the real capture path (a `BesiegeCityCommand` that takes its last city). Then `ProposeTradeCommand`, `ProposeAllianceCommand` and `AcceptPendingOfferCommand` against it are each rejected with that code and change nothing.
  4. **Relations are kept as history.** A test eliminates a nation that holds a `Trade` or `Alliance` relation and shows the relation matrix entry unchanged afterwards.
  5. **One thaw flag (#200 N4).**
     - `BugPolicyDiplomaticThaw` and `DiplomaticThawPolicy` are deleted, with the key and its `_provenance` in all three rulesets.
     - `git grep -n 'DiplomaticThaw' -- src tests data` prints nothing. The design-level name `bugPolicy.diplomaticThaw` in the docs and in `corpus.json`'s note names the policy, not the field, and stays.
     - `FaithfulThawColumnBug` is unchanged in value in every preset, and `QuarterlyThawSystem`'s tests are unchanged.
  6. **The test tidy (#200).** R2-F1: the remark says only 360 is a non-multiple of 100. R2-F2: the summary describes its own test. R2-F4: a test drives `UnknownProposer`.
  7. **The dead guard (#254).** The `EndsWith("._provenance")` guard is removed. The preset comparison still fails on a mutated value two and three levels deep, including inside an array element; show both mutations in the PR.
  8. `dotnet build IC2.sln` and `dotnet test IC2.sln` green, and the diff lists only Owns paths.
- **Hazards**:
  - **Do not break or delete anything on elimination.** The user decided that relations stay as history. Deleting the eliminated nation's record would dangle turn-order and diplomacy ids (T17's reasoning); this task only rejects new diplomacy with it.
  - **Deleting a required field touches all three rulesets at once.** `classical-faithful.json` changes through the exporter only; a hand edit fails `ExportScriptReproducibilityTests`. `ImprovedPresetTests`' preset-difference set loses `flags.bugPolicyDiplomaticThaw`, and nothing else changes in it.
  - **Docs affected.** `game-design.md:80`, `design-audit.md:242`, `build-process.md:381` and `release-plan.md:158` name the design-level policy `bugPolicy.diplomaticThaw`, and T63's entry cites it as the pattern for its own flag. List them in "Docs affected"; do not edit them.
  - **`PendingOfferSystem` already skips eliminated candidates** (`aliveOk`, ~:96). The accept path is the gap, not the roll.
  - **Line numbers are at `7e18d83`**, after T63's merge, which also edited `ImprovedPresetTests.cs` and `Ruleset.cs`.

---
