#### T67 Siege state as a predicate, not a flag

- **Design milestone**: none — a correction to merged T17 and T18 code, bugs [#220](https://github.com/diegoami/imperial_conquest_2/issues/220) and [#211](https://github.com/diegoami/imperial_conquest_2/issues/211). **Labels**: `phase:2 lane:engine`
- **Branch**: `task/T67-siege-predicate` · **Model/effort**: **Opus / High** · **Reviewer**: **Opus / High**
- **Start after**: T63, T68 · **Merge after**: T63, T68 (T63, merged, added the per-attempt fortification strip and changed `CityCaptureResolver.cs`; T68 makes the exporter own preset-specific provenance, and this task's `cityOrders` provenance reaches `classical-faithful.json` only through the exporter)
- **Owns**:
  - `src/IC2.Engine/Model/GameState.cs` (`CityState.UnderSiege` only) and `src/IC2.Engine/Model/GameStateFactory.cs` (its initialisation at ~:103 only)
  - `src/IC2.Engine/Cities/Orders/**`, `tests/IC2.Engine.Tests/Cities/**`
  - `src/IC2.Engine/Cities/Capture/CityCaptureResolver.cs` — **the cascade gate only** (~:309–321) and the private distance helper it calls (~:432–433)
  - `src/IC2.Engine/Ai/AiEconomyPhase.cs` — **the two `UnderSiege` reads only** (~:157, ~:359); `tests/IC2.Engine.Tests/Ai/AiCommandLegalityTests.cs` — the fixture at ~:239 only
  - `data/rulesets/*.json` — **the `cityOrders._provenance` only**; `classical-faithful.json` regenerated through the exporter
  - **Only if Done-when 2 deletes the field:**
    - the `UnderSiege:` argument, and nothing else, in the test files that construct a `CityState`: `Armies/MobilizationReceivingArmyTests.cs`, `Battle/BattleTestbed.cs`, `Battle/SiegeBattleTests.cs`, `Economy/CityOwnershipTaxTransferTests.cs`, `Model/GameStateSerializationTests.cs`, `Model/ToyFixtures.cs`, `Naval/BuyFleetSupplyCommandHandlerTests.cs`, `Recruitment/MobilizationFixture.cs` (all under `tests/IC2.Engine.Tests/`);
    - `src/IC2.Engine/Persistence/**` and `tests/IC2.Engine.Tests/Persistence/**`, **for the save-format migration only**; `tests/fixtures/saves/**` for a new-version fixture only, and `toy-3city-turn-10.v1.json` stays as it is.
- **Scope**: **`CityState.UnderSiege` is written by nothing.** `GameStateFactory` sets it `false` and no code ever sets it `true`, so every reader is unreachable in a real game. It is tested only through fixtures that set the flag by hand. The readers on `main` at `7e18d83`, after T63's merge:

  | Reader | Where | Owner |
  | --- | --- | --- |
  | T17's cascade gate, "if not already contested" (`FUN_0044ba1c`) | `CityCaptureResolver.cs` ~:309–313 | T17 |
  | T18's fortify refusal | `OrderCityCommandHandler.cs` ~:71 | T18 |
  | T18's weekly strip branch | `CityOrderProgressSystem.cs` ~:69 | T18 |
  | the AI's two reads | `AiEconomyPhase.cs` ~:157 and ~:359 | T22 |

  T54's implementer proposed the likely root cause: **the original stores no siege flag at all.** The weekly fortification tick is gated on `FUN_004497cc`, the `[confirmed]` hostile-army-adjacent predicate, which this engine already implements as `Economy/HostileArmyAdjacent`. The strip happens at the siege attempt itself, which T63 has now ported. If that holds, the flag is an invention standing in for a predicate, and it should go.

  **Research leads**, to read and not to edit: [`city-population-growth.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/city-population-growth.md) (~:59 defines `FUN_004497cc`; ~:98 gates the weekly fortification tick on it) and [`decompiled-unit-map-orders-and-record-fields.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-unit-map-orders-and-record-fields.md) (~:107, the refusal *"You cannot fortify a city which is under siege."*).

  **#211** (T18's review): N1, the worked example at `CityOrderProgressSystem.cs:20` reads *"599"* where the value is 595; N2, the order-placement cap at `OrderCityCommandHandler.cs:80` has no boundary test (mutating `>` to `>=` passed the whole suite); N3, `cityOrders._provenance.orders` does not name the `[open]` completion-at-100 reading.
- **Done when**:
  1. **Stage 1, research, before any code.** The PR's first section answers, for **each** of the four readers, whether the original reads a stored flag or evaluates `FUN_004497cc` (or another predicate), citing report lines or addresses, and states what was searched. A reader the reports do not settle is `[designed]`, and the PR says what was searched. If settling one needs new decompilation, **stop and report**: the main session runs it through the evidence pipeline.
  2. **Then one of two outcomes, stated in the PR:**
     - (a) **The flag goes.** `CityState.UnderSiege` is deleted, and every reader evaluates the predicate stage 1 found. Afterwards, `git grep -n -w UnderSiege -- src` prints nothing. `CityOrderRule.RefusedWhileUnderSiege` and its rejection code are rule data, not the flag, and `-w` does not match them.
     - (b) **The flag gets a producer.** The PR names the code that sets and clears it, with the evidence, and says which task's files that is.
  3. **Every reader on a real path.** For each reader, a test reaches it through a real command or system run from a state built by play (an enemy army moved next to the city), never from a hand-set flag. That covers:
     - the fortify refusal;
     - the cascade gate excluding a contested city after a real siege;
     - the weekly tick's branch;
     - both AI reads, with `AiCommandLegalityTests`' fixture (~:239) rebuilt on the real path.
     If a reader has no evidence behind it at all, the PR says so and the main session decides; it is not deleted silently.
  4. **The save format, if (a) changes it.** `SaveFormat.CurrentVersion` rises by one, and `SaveMigrations` gains a real step that drops the field, in T20's format. The committed `tests/fixtures/saves/toy-3city-turn-10.v1.json`, which carries `"underSiege": false` on every city (~:102, ~:126, ~:150), still loads through the migration chain. The PR says the format changed and why.
  5. **#211.** `CityOrderProgressSystem.cs:20` reads 595. An `OrderCityCommandHandler` test orders exactly `MaxOrderablePoints` (accepted) and one more (rejected), and mutating `>` to `>=` at ~:80 fails it. `cityOrders._provenance` names the `[open]` completion-at-100 reading in all three rulesets.
  6. **One distance metric at the cascade gate.** `CityCaptureResolver`'s private `ChebyshevDistance` (~:432–433, called at ~:318) is gone, and the gate calls `LandingTile.ChebyshevDistance` (from T70's exclusion of `Cities/Capture/**`).
  7. `dotnet build IC2.sln` and `dotnet test IC2.sln` green, and the diff lists only Owns paths. T22's soak keeps its invariants, and any seeded value that moves is listed with its old value, new value and cause.
- **Hazards**:
  - **Deleting a model field is the delete class.** Sweep every constructor call, every `with { UnderSiege = … }`, the save JSON and the fixtures. A field left in a v2 save that the loader no longer knows is an `UnknownFieldException` on load: a save that cannot be reloaded.
  - **`CityOrderRule.RefusedWhileUnderSiege` is a ruleset field, not the flag.** It stays; renaming it is not granted.
  - **T63's per-attempt strip (#293) is separate.** After T63, a siege attempt strips a pending order itself. Whether T18's **weekly** strip branch has any evidence is exactly what stage 1 answers for that reader; `city-population-growth.md` ~:98 suggests the weekly tick is *gated*, not stripped.
  - **Read `Economy/HostileArmyAdjacent.cs` and call it, but do not change it** (T37's). If it needs a new overload, stop and report.
  - **T65 also edits `AiEconomyPhase.cs`, and T71 works in `tests/IC2.Engine.Tests/Persistence/**`.** Never in flight together: whichever merges second rebases.
  - **Line numbers are at `7e18d83`**, after T63's merge.

---
