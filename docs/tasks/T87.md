#### T87 The leader falls, a human seat is handed over, and a dead nation is reborn

- **Design milestone**: none. It corrects items 4 and 6 of [#368](https://github.com/diegoami/imperial_conquest_2/issues/368), bug [#380](https://github.com/diegoami/imperial_conquest_2/issues/380) and follow-up [#374](https://github.com/diegoami/imperial_conquest_2/issues/374). It was planned in the triage of 2026-09-25, where the user split the work into two tasks after T85: T86 (conquest) and this one. **Labels**: `phase:2 lane:engine`
- **Branch**: `task/T87-leader-falls-rebirth` · **Model/effort**: Sonnet / High · **Reviewer**: **Opus / Medium**
- **Start after**: T86 · **Merge after**: T86 (it uses T86's conquered-by and conquest path). It is **never in flight with T79**, which moves AI weights that the AI deposition reads.
- **Owns**:
  - `src/IC2.Engine/Economy/Deposition.cs`, `HumanDepositionSystem.cs` and `AiDepositionHandler.cs`, and their tests under `tests/IC2.Engine.Tests/Economy/**`
  - `src/IC2.Engine/Economy/QuarterlyCityEconomySystem.cs` and `CityLoyaltyDraws.cs`: **only** the rebellion's call to rebirth; a new `src/IC2.Engine/Economy/Rebirth.cs`
  - `src/IC2.Engine/Presentation/GameSession*.cs`: **only** the message shown when the human's leader falls or the nation is conquered
  - `src/IC2.Engine/Diplomacy/RelationTransitions.cs`: **only** the `DiplomacyRejections` remark (#374)
  - `src/IC2.Engine/News/NewsMessageCatalog.cs`: **only** the deposition line, if it differs
  - any existing test elsewhere whose **setup** assumed the old timing, named in the PR
- **Scope**: Research [`decompiled-elimination-cleanup.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/decompiled-elimination-cleanup.md) §3 and §4 (research `43a44a1`) is the source for every rule below; each one is **[confirmed: decompile]** unless it is tagged otherwise.
  - **The leader falls: `FUN_0044C8F0`** (:50761). It first splits on who holds the seat:
    - **An AI seat:** the news line "*<nation> depose their leader <leader>.*".
    - **A human seat:** the `THumanFalls` screen, reading "*Your nation has been conquerred by <X>.*" when conquered-by is set. The seat's human flag is then cleared, and the seat goes to the computer. If no human seat is left, the game is over.

    Then, for either kind of seat, the fall itself:
    - a new leader, drawn from the nation's 12 names;
    - `unity = max(unity, min(550, unity + 150))`;
    - `treasury = treasury < 0 ? 0 : treasury + 1000`;
    - every relation between −5 and −1 is set to 0.

    Check the engine's deposition against every step.
  - **When a human's leader falls: `FUN_00452034`**, at the start of the human's turn. The triggers are 250 BC, 334 cities, unity below 400, or debt. The engine deposes only after the human types `end` (#380). Move the check to the start of the turn, before any order.
  - **Rebirth: `FUN_0044C360`** (:50507), called from the quarterly rebellion `FUN_0044C204`. A city rebels when it is not a capital, its loyalty is below 30, and its allegiance is a dead nation (unity below 1). The dead nation then comes back if **more than 7** cities with that allegiance have loyalty below 40. On rebirth:
    - unity 450, conquered-by −1, and treasury, tax base and city count 0;
    - its 40 slots' troops 0, and its relation row all 0;
    - a new leader;
    - each such city defects to it, with relation −8 towards its former owner;
    - the strongest such city becomes its capital;
    - it is no longer eliminated.
  - **#374.** The `DiplomacyRejections` remark overstates two facts:
    - A human eliminated by defection ends at unity 150, not 0 (**[derived]**, the order quirk).
    - `TPolitics_ChangeIR` gates four actions, not five.

    Correct the remark. The engine keys "alive" on `Eliminated`, so the unity-150 quirk is **not** reproduced; the remark says so.
- **Done when**:
  1. **The fall.** A test per step of `FUN_0044C8F0`, for an AI seat and a human seat, with the constants as ruleset data citing the report.
  2. **Timing (#380).** A human in debt is deposed at the start of its turn, before it can issue an order. A test issues an order after `end` and shows it rejected, because the seat is no longer human. Each of the four triggers has a test.
  3. **The human seat.**
     - The seat goes to the AI, and the CLI shows the fall message: the conquered text when conquered-by is set, and a deposition text otherwise.
     - T83's lost-seat fallback to watch mode still holds.
     - When no human seat remains, the session reports that the game is over. The PR states how.
  4. **Rebirth.**
     - One test shows the 7/8-city boundary.
     - One test shows every reset field, the defections and the capital choice.
     - A reborn nation takes turns again, and diplomacy can target it again.
     - Rebirth adds no armies or fleets.
     - The loyalty and rebellion draws go through `IRng`.
  5. **#374's remark** is corrected.
  6. **Seeded outcomes.** Every seeded test, measurement or golden that changes is listed with its reason. Goldens are regenerated under [build-process.md §2.3](../build-process.md#2-how-the-build-avoids-conflicts)'s conditions.
  7. `dotnet build IC2.sln` and `dotnet test IC2.sln` green, and the diff lists only Owns paths.
- **Hazards**:
  - **Rebirth reverses elimination.** It must undo `Eliminated` without undoing anything that should stay done. T69's reset relations become the reborn row of zeros and −8s; T84's deleted forces stay deleted. Probe with two dead nations, one reborn and one not.
  - **A reborn human seat.** The original's seat stays with the computer after the fall. A reborn nation that was once human is played by the AI; the PR says so.
  - **The city's `Allegiance` field** drives rebirth. It exists on `CityState`. If it is not kept up to date the way the original's `+0x14` is, stop and report.
