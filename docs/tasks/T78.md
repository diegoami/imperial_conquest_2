#### T78 Test and import hygiene: guard precision, scratch isolation, untested boundaries, and a tombstoned fleet's cargo

- **Design milestone**: none. It collects the non-blocking follow-ups of T21, T65, T68, T70 and T77, and bug #353: [#332](https://github.com/diegoami/imperial_conquest_2/issues/332), [#334](https://github.com/diegoami/imperial_conquest_2/issues/334) (the test items), [#340](https://github.com/diegoami/imperial_conquest_2/issues/340), [#342](https://github.com/diegoami/imperial_conquest_2/issues/342), [#351](https://github.com/diegoami/imperial_conquest_2/issues/351) and [#353](https://github.com/diegoami/imperial_conquest_2/issues/353). **Labels**: `phase:2 lane:data local-only`
- **Branch**: `task/T78-test-import-hygiene` · **Model/effort**: Sonnet / Medium · **Reviewer**: **Opus / Medium**
- **Start after**: — · **Merge after**: —. It is **never in flight with T75** (the `Export/**` tests), **T76** (it owns `src/IC2.Engine/Import/**`'s mercenary mapping), **T79** (the AI tests), **or T82** (both edit the #256 test in `GameSessionCommandsTests.cs`). The triage of 2026-09-25 folded in [#372](https://github.com/diegoami/imperial_conquest_2/issues/372) and [#382](https://github.com/diegoami/imperial_conquest_2/issues/382)'s CI item.
- **Owns**:
  - `tests/IC2.Engine.Tests/Export/NoToyProvenanceWordingTests.cs` (#332)
  - `tests/IC2.Data.Tests/SkippableFactGuardTests.cs` and `tests/IC2.Engine.Tests/SkippableFactGuardTests.cs` (#342)
  - `tests/IC2.Engine.Tests/Core/Determinism/DeterminismGuardTests.cs`: **only** the scratch-writing test and its collection (#353); and `tests/IC2.Engine.Tests/Battle/BattleDeterminismTests.cs`: **only** T74's `IsKnownTestScratch` filter, if #353's fix lets it go
  - `tests/IC2.Engine.Tests/Economy/Commands/BuySupplyCommandHandlerTests.cs` and `tests/fixtures/corpus.json`: **only** the `command.adjacencyRadiusTiles` note (#351)
  - `tests/IC2.Engine.Tests/Presentation/GameSessionCommandsTests.cs`: **only** the #256 dash-wrapped-round test; and `tests/IC2.Engine.Tests/Ai/AiClassicalMediterraneanStallMeasurementTests.cs` (#334 N4–N7)
  - `src/IC2.Data/SaveFleetTable.cs`: **only** `SkippedFleetRecord` (a `CarriedArmyIndex`); `src/IC2.Engine/Import/EmbarkationLinker.cs`, `src/IC2.Engine/Import/OriginalSaveFieldMapping.cs` (that field's entry only), `tests/IC2.Data.Tests/FleetTombstoneTests.cs`, `tests/IC2.Engine.Tests/Import/EmbarkationLinkerTests.cs` and `tests/IC2.Data.Tests/CorpusFixtures/ArmyFleetFieldRangeSweepTests.cs` (the two fleet bounds only) (#340)
  - `src/IC2.Engine/Model/GameStateFactory.cs`: **only** T75's starting-news helper (#372 N6, N7); `src/IC2.Engine/Model/World.cs`: **only** the doc comments of the two starting-data fields (#372 N5); `tests/IC2.Engine.Tests/Model/WorldStartingDataValidationTests.cs` and `GameStateFactoryStartingDataTests.cs` (#372)
  - `.github/workflows/ci.yml`: **only** a `timeout-minutes` on the test job (#382)
  - **Added 2026-09-25 after the implementation (PR #395) met two places the list missed:**
    - `src/IC2.Engine/Import/OriginalSaveImporter.cs`: **only** passing the skipped fleets' claims (`fleetTable.SkippedRecords`) to `EmbarkationLinker`, so that Done-when 6's unlink runs in the real import (#340 N1)
    - `src/IC2.Engine/Model/Ruleset.cs` (~:297) and `data/rulesets/*.json`: **only** the `commandAdjacencyRadiusTiles` remark and `_provenance` strings that say the corpus id "prescribes" the radius (#351 R2). `classical-faithful.json` changes through the exporter only.
- **Scope**: Every item was reported by a reviewer, is non-blocking, and has its evidence on its issue. Read each issue before starting.
- **Done when**:
  1. **The toy-wording exemption is exact (#332).** `NoToyProvenanceWordingTests` exempts the root `_provenance.id` of `classical-faithful.json` only. A "toy" note at `improved.json`'s root `_provenance.id` fails the test; show that mutation.
  2. **The IL guard walks real instructions (#342).** Both `SkippableFactGuardTests` decode IL by the opcode table's operand sizes (`System.Reflection.Emit.OpCodes`), never byte by byte. A throwaway `[Fact]` whose IL holds a `0x28` or `0x6F` byte inside an earlier operand, followed by a real `Skip.IfNot` call, still fails the guard. Show it, then remove the throwaway.
  3. **The scratch writer is isolated (#353).** `DeterminismGuardTests`' scratch-writing test either joins the one collection every `src/**` scanner uses, or writes its scratch file somewhere no scanner reads. Show that a full run leaves `git status --porcelain --ignored` over `src/` and `godot/` unchanged. If T74's name filter in `BattleDeterminismTests` is no longer needed, remove it. Either way, the guard still fails on a real reference to a candidate type; show that mutation.
  4. **The fleet-provider boundary is tested (#351 R1).** A test places the provider fleet at distance 1 and shows the purchase accepted. The mutation that rejects distance 1 at `BuySupplyCommandHandler.cs` ~:188 fails it. The corpus note of `command.adjacencyRadiusTiles` names the mobilization receiving-army `d == 1` test correctly, and the provenance stops saying the corpus id "prescribes" it (#351 R2).
  5. **The #256 test and the stall harness (#334 N4–N7).**
     - The #256 test gives the log a news-producing step before round 1, plus a round-1 count check, so a fixed "last N entries" mutation fails for every N, 9 included.
     - The "Round 1: the approach march only" comment is corrected.
     - The stall harness's turn-cap remark gives the right season-refill turns (t97–112) and no longer extrapolates past the cap.
     - "No affordable action" is printed only after comparing the budget with the cheapest unit and fortification point.
  6. **A tombstoned fleet's cargo (#340 N1).**
     - `SkippedFleetRecord` carries the record's `CarriedArmyIndex`.
     - The import unlinks a surviving army that a skipped fleet claimed: it is no longer marked aboard, and it keeps its position.
     - A synthetic test shows both the unlink and the position **through `OriginalSaveImporter`**, not only through `EmbarkationLinker`, and the declared field mapping gains the new property.
     - No corpus save has this shape; the PR says so.
  7. **Two loose bounds (#340 N5).** `ArmyFleetFieldRangeSweepTests`' fleet `Moves` and `CarriedArmyIndex` bounds come from evidence: a cited cap or the field's documented range, never the observed maximum. The PR cites each.
  8. **T75's news-seed checks (#372).**
     - A slot of exactly the byte limit passes (N1).
     - A byte below `0x20` is rejected (N2).
     - The two remarks on non-contiguous relation codes and order sensitivity each have a test (N3).
     - The full-round test asserts the round header appears exactly once (N4).
     - `World.cs`'s doc comments name the current methods (N5).
     - **A starting news seed with more slots than the ruleset's ring is rejected at game creation** with a typed error, by the user's decision of 2026-09-25 (N6).
     - The two error messages name the world, not the ruleset, and say "character" where they count UTF-16 code units (N7).
  9. **A CI hang guard (#382).** The test job has a `timeout-minutes` generous enough for today's slowest green run. The PR states the value and the run it measured.
  10. `dotnet build IC2.sln` and `dotnet test IC2.sln` are green with and without the original files, and the diff lists only Owns paths.
- **Hazards**:
  - **Each item is small; keep each one small.** No refactor beyond the finding.
  - **The IL walk must not start flagging `[SkippableFact]`.** Re-run the T77 mutations after the change.
  - **Moving a test between collections changes ordering.** Run the full suite several times, and state the count.
- **Constraint**: `local-only`. Done-when 6 and 7 touch real-save sweeps.
