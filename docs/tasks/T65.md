#### T65 AI hygiene, the missing AI tests, and the classical-mediterranean stall, measured

- **Design milestone**: none — follow-ups to T22, T57 and T60, bugs [#237](https://github.com/diegoami/imperial_conquest_2/issues/237), [#261](https://github.com/diegoami/imperial_conquest_2/issues/261), [#272](https://github.com/diegoami/imperial_conquest_2/issues/272) and [#256](https://github.com/diegoami/imperial_conquest_2/issues/256), plus a measurement for [#268](https://github.com/diegoami/imperial_conquest_2/issues/268). **Labels**: `phase:2 lane:engine`
- **Branch**: `task/T65-ai-hygiene` · **Model/effort**: Sonnet / High · **Reviewer**: **Opus / Medium**
- **Start after**: T63 · **Merge after**: T63 (T63 changes battle outcomes, so the soak and every AI seed this task pins must run on the corrected resolver)
- **Owns**:
  - `src/IC2.Engine/Ai/**`, `tests/IC2.Engine.Tests/Ai/**`
  - `tests/IC2.Engine.Tests/Presentation/GameSessionCommandsTests.cs` — **the new #256 test only**
  - `src/IC2.Engine/Presentation/GameSession.cs` — **only the comment above `HandleEnd`'s `newsBefore` line** (~:367–382), which names #256 as open and must point at the new test instead
- **Scope**: Every item here is in T22's former Owns list, which no ready task holds.

  - **#237.** N1: the invariant-culture helper `Inv(...)` is copy-pasted in five files (`AiMilitaryPhase.cs:600`, `AiEconomyPhase.cs:469`, `AiDiplomacyPhase.cs:196`, `AiTurn.cs:292`, `AiGameRunner.cs:254`). T22 Done-when 4 needs byte-identical logs across locales, and a later edit to four of the five copies would silently break that. N2: `AiSoakTests` runs the 50-seed soak twice, once per `[Fact]` (`:45` and `:91`, each calling `RunSoak()` at `:123`).
  - **#261** (T57's review). O1: `AiEconomyPhaseMobilizationTests` builds `Propose(...)` on `OpenWorld(30, 30)` (`:43`) while `AiScriptedStates.DriveOneTurn` uses the 8×6 toy world, and `receiving-army` at `(6, 6)` (`:191`) lies outside the toy world; the class remarks explain only one world. O2: a literal `Assert.Equal(6, mobilized.Quality)` (`:352`) sits beside the derived assertion. O3 is PR-body text and needs no action.
  - **#272** (T60's review). N2: the fleet half of the resupply guard that declines a zero-ton pairing has no targeted test; both `AiResupplyPassTests` cases for it empty `Fleets`. N3: `AiTurn`'s "one tally sample per turn" invariant (`:134–135`) is untested, and the 23,900 adjacency-pair figure rests on it. N1 and N4 are inaccurate remarks. N5 and N6 are PR-body notes and need no action.
  - **#256** (T23 Done-when 3's remaining sub-case). No test covers `HandleEnd`'s round read when the round contains a **dash-wrapped** elimination. A human capture is flushed before `newsBefore` is read, so only an AI seat that besieges, captures and eliminates within its own turn reaches it. T57, the blocker, has merged.
  - **#268, measured and not fixed** (user decision, 2026-09-23: "measure first"). Every seed tried on the shipped all-AI `classical-mediterranean` scenario reports a stall run of 2, which T22's Done-when 1 would fail. T22's soak never ran that scenario. Which seats stall, when, and why is unknown, and the fix may belong in the AI, the assertion or the scenario.
- **Done when**:
  1. **One `Inv` (#237 N1).** One `internal static` helper, `AiFormat.Inv`, in `IC2.Engine.Ai`, and all five call sites go through it. `git grep -n 'static string Inv(' -- src` prints exactly one line. The AI determinism tests and the soak's per-seed logs are byte-identical before and after.
  2. **The soak runs once (#237 N2).** Both `AiSoakTests` facts read one shared result, from an xUnit class fixture or an equivalent once-per-class cache, and the PR shows the soak running once per test run. The five-minute budget assertion still measures that run. A soak that throws still fails both facts.
  3. **The two untested branches (#272 N2, N3).**
     - A targeted `AiResupplyPassTests` case covers the fleet half of the zero-ton guard; deleting that half fails it.
     - A test pins one `AiSiegeGateTally` sample per turn; feeding the tally on every action pass fails it.
     - Show both mutations in the PR.
  4. **The remarks (#272 N1, N4).** `AiResupplyPass.cs` (~:198) no longer says an army spends its purse only when buying supply at a foreign city; it names `MercenaryDesertion` and `SupplyPurchase`. `AiSiegeGateTally.RulesetCannotSiege`'s summary (~:49–52) says it counts once per army per turn.
  5. **The mobilization tests (#261 O1, O2).** The class remarks of `AiEconomyPhaseMobilizationTests` explain both worlds and that `receiving-army` lies outside the toy world. The literal quality at `:352` is either kept, with a remark saying it anchors the derived expression, or derived from the ruleset; the PR says which.
  6. **The dash-wrapped round (#256).**
     - A test in `GameSessionCommandsTests` drives a scripted AI seat to besiege, capture and **eliminate** a nation within one AI turn, then asserts `HandleEnd` prints every entry the round appended: the header, the dash-wrapped elimination lines and an ordinary line.
     - Changing `HandleEnd` to print a fixed number of trailing entries fails it.
     - The `GameSession.cs` comment above `newsBefore` names the new test instead of #256.
  7. **The stall, measured (#268).**
     - The shipped all-AI `classical-mediterranean` scenario is run over seeds 1–10.
     - The PR body reports, for each seed, which nations stall, at which turns, each run's length, and the cause as found: no affordable action, no legal command, or something else, named. It also gives the command that reproduces the run. The same report goes as a comment on #268.
     - Any committed harness asserts nothing about stall length, so it cannot fail on the defect it measures, and it stays inside the test suite's time budget.
     - **Nothing is fixed.** The AI's behaviour, T22's soak assertions and the scenario data are unchanged by this line.
  8. **No behaviour change elsewhere.** Done-when lines 1–6 change no AI decision: the soak's per-seed outcome lines are identical before and after.
  9. `dotnet build IC2.sln` and `dotnet test IC2.sln` green, and the diff lists only Owns paths.
- **Hazards**:
  - **`AiFormat.Inv` must stay byte-identical.** One culture, one format provider, no trimming. The determinism test is the check, not a reading of the code.
  - **A cached soak must not swallow a failure.** If the fixture caches a thrown exception or a partial report, both facts must still fail with it.
  - **#268 is a measurement.** Do not relax T22's stall assertion, point the soak at `classical-mediterranean` or change the scenario. #267 (the toy world cannot be besieged) is open for after `v0.3.0`, and the two are triaged together.
  - **Other tasks also touch `src/IC2.Engine/Ai/**`.** T66 may touch `AiMilitaryPhase.cs` (the archer pointer's callers only), and T67 `AiEconomyPhase.cs` (two `UnderSiege` reads only). Whichever merges second rebases.
  - **Line numbers are at `e3e0a2f`.** T63 edits none of these files, but re-read each one before editing.

---
