#### T77 Local-only tests skip, never fail, without the original files

- **Design milestone**: none. It is a correction for bug [#336](https://github.com/diegoami/imperial_conquest_2/issues/336), in T73's merged tests. **Labels**: `phase:2 lane:data`
- **Branch**: `task/T77-skippable-facts` · **Model/effort**: Haiku / Low · **Reviewer**: Sonnet / Medium
- **Start after**: — · **Merge after**: —. It runs **after T21 and before the v0.3.0 freeze**, by the user's decision of 2026-09-24: the milestone's claim C1 says every skip is named, and today a clone without the original files fails these three tests.
- **Owns**:
  - `tests/IC2.Data.Tests/SaveTurnOrderTests.cs` and `tests/IC2.Data.Tests/SaveNewsLogTests.cs`: **only the test attributes** of the three methods below
  - a new guard test file in each test project: `tests/IC2.Data.Tests/SkippableFactGuardTests.cs` and `tests/IC2.Engine.Tests/SkippableFactGuardTests.cs`
- **Scope**: Three tests call `Skip.IfNot(...)` but are declared `[Fact]`, so without `assets.local.ini` (and without `IC2_FIXTURES_DIR`) they fail instead of skipping:
  - `SaveTurnOrderTests.cs:33`, `On_the_configured_machine_thracia_is_at_index_5_of_16_in_the_named_save`
  - `SaveTurnOrderTests.cs:83`, `The_dat_has_no_trailer_at_all`
  - `SaveNewsLogTests.cs:185`, `On_the_configured_machine_the_dat_seed_matches_the_report_and_slots_27_to_39_are_empty`

  A scan of every `tests/**/*.cs` on 2026-09-24 found exactly these three and no others. CI stays green only because it sets `IC2_FIXTURES_DIR`.
- **Done when**:
  1. **The three skip.** Each of the three is `[SkippableFact]`. With `assets.local.ini` absent and `IC2_FIXTURES_DIR` unset, `dotnet test IC2.sln` reports them as skipped, each with its reason, and **zero failures**. Show the run in the PR, with the ini removed and then restored.
  2. **A guard in each test project.** A test reflects over its own assembly and fails, naming the method, if any method marked `[Fact]` or `[Theory]` (not `[SkippableFact]` or `[SkippableTheory]`) calls `Skip.If` or `Skip.IfNot`. It inspects the method body's IL or calls into `Xunit.Skip`, not the source text. Show both mutations in the PR: turning one of the three back to `[Fact]` fails the Data guard, and a throwaway `[Fact]` calling `Skip.IfNot(false, …)` in the engine project fails the Engine guard. Remove the throwaway afterwards.
  3. `dotnet build IC2.sln` and `dotnet test IC2.sln` are green with the ini present, and the diff lists only Owns paths.
- **Hazards**:
  - **Async and local functions.** If a test calls `Skip.IfNot` from a helper, from a lambda or after an `await`, the call is not in the test method's own IL. Say what the guard covers. If it can't see helpers, say so, and check by hand that no helper-routed skip exists today.
  - **Don't change what the tests assert.** Only the attributes change. Every assertion and every skip reason stays exactly as it is.
