#### T66 Battle follow-ups: the fleet-loss cap, the adjacency boundaries, and a ruleset pointer for archers

- **Design milestone**: none — follow-ups to T52 and T54, bugs [#187](https://github.com/diegoami/imperial_conquest_2/issues/187), [#222](https://github.com/diegoami/imperial_conquest_2/issues/222) (N2) and [#221](https://github.com/diegoami/imperial_conquest_2/issues/221), with the user's decisions of 2026-09-23 on #221. **Labels**: `phase:2 lane:engine`
- **Branch**: `task/T66-battle-follow-ups` · **Model/effort**: Sonnet / High · **Reviewer**: **Opus / Medium**
- **Start after**: T63, T68 · **Merge after**: T63, T68 (T63, merged, rewrote the casualty call sites this task tests; T68 makes the exporter own preset-specific provenance, and this task adds a ruleset field that reaches `classical-faithful.json` only through the exporter)
- **Owns**:
  - `src/IC2.Engine/Battle/**` and `tests/IC2.Engine.Tests/Battle/**`, **excluding `Candidates/`** in both (T59's)
  - `tests/IC2.Engine.Tests/Armies/**` — **the relocated disband tests only** (Done-when 7), in a new file
  - `src/IC2.Engine/Model/Ruleset.cs` and `src/IC2.Engine/Model/GameDataSchema.cs` — **the archer pointer field only**
  - `data/rulesets/*.json` — **that key and its `_provenance` only**; `classical-faithful.json` regenerated through `scripts/export-classical-world.cs`, never edited by hand
  - `src/IC2.Engine/Ai/AiMilitaryPhase.cs` — **only the callers of that pointer**, and only if its accessor's signature changes
  - **Folded from [#327](https://github.com/diegoami/imperial_conquest_2/issues/327), 2026-09-24:** `data/rulesets/toy-ruleset.json`, **only** `combat.detailedResolver._provenance.typeEffectiveness` (with `classical-faithful.json` regenerated through the exporter); and `tests/fixtures/corpus.json`, **only** the `matrix.orientationCaveat` entry and the `note` of `matrix.lightInfantry.vs.lightInfantry` (~:256).
- **Scope**: Follow-ups from T52's and T54's reviews, all in `Battle/**`, which no ready task owns now that T63 has merged.

  - **#187** (T52's review). N1: `BattleCasualties.ApplyToFleet`'s `Math.Min(ships, lost)` cap (`BattleCasualties.cs:267`) is untested; removing it passed all 72 Battle tests. N2: its documented "still draws" behaviour for `ratio <= 0` is untested, and its reachability was never proved; moving the draw after the check passed everything. Determinism is the stake. N3: a stray blank line in `ResolveField`.
  - **#222 N2** (T54's review). Only the army gate has an adjacency boundary theory. The siege and naval `NotAdjacent` fixtures sit 4 tiles apart (`BesiegeCityCommandTests` ~:229–233, `AttackFleetCommandTests` ~:126–130), so they pass at radius 1, 2 or 3 alike. #222 N1 (disband calling `AttackLegality.AreAdjacent`) is already done on `main` (`DisbandArmyCommandHandler.cs` ~:74–87).
  - **#221, with the user's decisions of 2026-09-23.**
    - **N2:** the two-command form (`DeclareWarCommand`, then the attack) stays. T16's guard `FieldBattleTests.AssertNoBattleSourceReferencesDiplomacy` (~:385) keeps scanning all of `src/IC2.Engine/Battle/**`, and its remark states the rule, so the next implementer meets a stated rule rather than an unexplained failure. The scan is **not** narrowed.
    - **N1:** a `Ruleset` pointer field for the **archer unit type only**, replacing the `BattleCommandRuleset.ArcherUnitTypeId = "archers"` literal (`BattleCommandRuleset.cs:58`) that callers thread today. The fortification order stays found **by its behaviour** (`CityOrderRule.WipedBySiegeAttempt`, via `BattleCommandRuleset.FortificationOrderIdIn`), with no pointer.
    - **#221's note:** T54's disband tests live under `Battle/Commands/` only because T54 had no `Armies` tests grant. This task has one, so it moves them.
  - **The Chebyshev copies inside `Battle/**`** (T70 consolidates the rest of the engine and excludes these): `AttackLegality.cs:83`, `ScatterPlacement.cs:122` and `:133`.
- **Done when**:
  1. **The fleet-loss cap (#187 N1).** A test calls `ApplyToFleet` where `(ships × ratio) / divisor` exceeds `ships` and asserts the result equals `ships`; removing the `Math.Min` fails it.
  2. **The `ratio <= 0` draw (#187 N2).** Either a remark proves the branch unreachable from every call site (today, only `InstantBattleResolver.cs` ~:528), naming each site and why its ratio is positive, or a test pins that `ApplyToFleet` with ratio `0` consumes exactly one draw and returns `0` (moving the draw after the check fails it). The PR says which. N3's blank line goes if it is still there.
  3. **Each adjacency gate fails at its own boundary (#222 N2).** The siege and naval `NotAdjacent` fixtures sit at Chebyshev distance **2**. Replacing the siege gate's adjacency check with "distance ≤ 2" fails a named siege test, and the same for the naval gate; show both mutations in the PR.
  4. **The guard states its rule (#221 N2).** `AssertNoBattleSourceReferencesDiplomacy`'s remark says that an attack on a nation not at war is two commands, `DeclareWarCommand` and then the attack, by the user's decision of 2026-09-23 (#221), so nothing under `Battle/**` calls diplomacy. The scanned paths are unchanged.
  5. **The archer pointer (#221 N1).**
     - One `Ruleset` field names the archer unit type. It is `"archers"` in all three rulesets, and its `_provenance` cites original type code 2 (`IC2.Data.UnitCatalog`) and the search T54 recorded.
     - The `BattleCommandRuleset.ArcherUnitTypeId` constant is gone, and `ArcherUnitTypeIdIn` reads the field. `git grep -n '"archers"' -- src/IC2.Engine/Battle src/IC2.Engine/Ai src/IC2.Engine/Strength` prints comment lines only. `Armies/ArmyNaming.cs`'s display-name table keeps its own `"archers"` key; it is a name table, not this pointer, and is outside Owns.
     - A ruleset whose pointer names an undeclared unit type still gets the existing typed rejection, tested.
     - No pointer is added for the fortification order.
  6. **One distance metric in `Battle/**`.** `AttackLegality.cs:83`, `ScatterPlacement.cs:122` and `:133` go through `LandingTile.ChebyshevDistance`, and `git grep -n -E 'Math\.Max\(\s*Math\.Abs' -- src/IC2.Engine/Battle` prints nothing. Every seeded scatter and battle test is unchanged.
  7. **The disband tests are relocated (#221's note).** T54's three disband tests (`CityTileRuleAndDisbandReachabilityTests`' `DoD04_*`, ~:86, ~:115, ~:131) move unchanged in body to a new file under `tests/IC2.Engine.Tests/Armies/`. The movement test `DoD03_AnArmyNeverEndsOnACityTile` stays, because no movement-test home is granted, and the remaining file's remark says so.
  8. **The presets.** `ExportScriptReproducibilityTests` is green on the regenerated `classical-faithful.json`, and `ImprovedPresetTests`' preset-difference set is unchanged: the pointer is equal in both presets.
  8a. **Folded bug ([#327](https://github.com/diegoami/imperial_conquest_2/issues/327), added 2026-09-24): the matrix orientation is confirmed.** [`combat-type-effectiveness-matrix.md`](https://github.com/diegoami/imperial-conquest-2-research/blob/main/docs/reports/combat-type-effectiveness-matrix.md) ~:19 resolves its caveat: rows are indexed by the attacker's type and columns by the defender's, from the melee function's index arithmetic. `typeEffectiveness`'s `_provenance` stops calling the orientation a candidate and cites that paragraph. `matrix.orientationCaveat` is corrected to the confirmed reading (tag `confirmed`), or removed if nothing cites it, with the reason in the PR. `matrix.lightInfantry.vs.lightInfantry`'s `note`, which still calls the axis labelling uncertain, is corrected the same way. A `jq` search of `corpus.json` and the three rulesets for "candidate orientation" or "uncertain" on a `matrix.*` or `typeEffectiveness` entry returns nothing. No value changes.
  9. `dotnet build IC2.sln` and `dotnet test IC2.sln` green, and the diff lists only Owns paths. No seeded outcome moves; if one does, the PR lists its old value, new value and cause.
- **Hazards**:
  - **A new required `Ruleset` field breaks every ruleset that lacks it.** `SchemaValidator` throws `MissingRequiredFieldException`. Add it to all three rulesets in one commit, `toy-ruleset.json` first and `classical-faithful.json` through the exporter. A hand edit fails `ExportScriptReproducibilityTests`.
  - **Do not add loader validation for the pointer.** `GameDataValidation.cs` is outside Owns. A dangling pointer is caught by the existing typed rejection; if that proves insufficient, stop and report.
  - **T63 rewrote most of these files** (merged as `562e608`): `BattleCasualties.cs`, `InstantBattleResolver.cs`, `AttackLegality.cs`, `FieldBattleTests.cs` and both command test files. Line numbers here are at `7e18d83`, after that merge. `ApplyToFleet`'s one production caller is `InstantBattleResolver.cs` ~:528.
  - **`Candidates/` is T59's**, in `src` and in `tests`.
  - **T65 also edits `src/IC2.Engine/Ai/**`.** Whichever merges second rebases.

---
